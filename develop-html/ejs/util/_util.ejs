<%# ルートからの距離に応じて(../)を出力する %>
<%# pathSet(filename)かpathSet(absPath)で使用する %>

<%
 pathSet = function(absPath){
	var rootPath = absPath.split('develop-html/ejs')[1];
	if(rootPath.split('/').length - 2 > 0){
		return '../'.repeat(rootPath.split('/').length - 2);
	}else{
		return '';
	}
 }
%>
